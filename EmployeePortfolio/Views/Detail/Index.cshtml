@model IEnumerable<EmployeePortfolio.Models.empInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.empno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.empname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.skills)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.alloc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.exper)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rmap)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.empno)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.empname)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.projname)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.skills)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.alloc)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.exper)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.rmap)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.empno }) |
        @Html.ActionLink("Details", "Details", new { id = item.empno }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.empno })
    </td>
</tr>
}

</table>
